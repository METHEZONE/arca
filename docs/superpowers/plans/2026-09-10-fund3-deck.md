# ARCA ZER01NE IR Deck (web) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Ship a Korean, 10-minute, single-file web pitch deck for the ZER01NE Sprint IR Day (2026-09-21, 투심위 6명) at `thezonebio.com/arcapitch`, and reply it tonight to mentor 김정수 (cc 제로원 매니저 3명) as the IR Deck 초안 he requested.

**Architecture:** One static `index.html` (Pretendard, Toss×Jeton tokens from `app/arca/arca.css`, CSS-only orange Spirit, IntersectionObserver reveals, scroll-snap slides, keyboard nav, print CSS for the 9/17 PDF submission) with resized/EXIF-stripped assets in `img/`. Source lives in the arca repo under `ir/arca-fund3/`; deployed copy lives in the thezonebio.com repo under `public/arcapitch/` behind a `beforeFiles` rewrite — same pattern as `/knotpitch`. No framework, no build step, so it also works as an email attachment.

**Tech Stack:** HTML/CSS/vanilla JS, ffmpeg (assets), Next.js rewrites (thezonebio.com), Vercel git deploy, Composio Gmail `GMAIL_REPLY_TO_THREAD`.

**Spec:** `intent/arca-zer01ne-fund3-narrative.spec.md` (slides §2.2, moat §2.1, demo §2.3). Intent: `intent/arca-zer01ne-fund3-narrative.intent.md`.

## Global Constraints

- Pitch is **10 min + 20 min Q&A** on 2026-09-21 10:20 (임유미 9/3 메일). IR 자료 제출 **9/17(목) 15:00**. The spec's 15-min/13-slide table compresses to 11 slides (S8+S9 merged, S12 folded into S13).
- Banned words anywhere in deck copy: `비타민`, `second self`, `Second Self`, `방패`, `flow-state guardian`. `second brain` only in a negating sentence.
- Every external number carries a source footnote (1차 출처 or 언론 인용 표기). No number without a source.
- Traction slide shows **today's facts only** and an explicit "9/21까지 채울 칸" — no projected numbers presented as actuals.
- Hardware: appears as input surface + attachment object only; funding share 10%.
- Ask: **5억 · 프리시드 · 12개월**. No valuation on any slide.
- Team: 박민성 · 박진성(HW) · 하승호(임베디드), knot = "Google Cloud × Solana AI Agentic Hackathon 3위 (2026-08-21)".
- Tokens: `--ink #191f28`, `--sub #4e5968`, `--ter #8b95a1`, `--line #f2f4f6`, `--field #f7f8fa`, `--accent #f73b20`, `--accent-press #e2331a`, radii 16/24, `--shadow-card 0 -4px 16px rgba(0,0,0,.04), 0 12px 32px rgba(0,0,0,.05)`, Spirit gradient hi `#ff9d6b` / mid `#f75b2b` / lo `#e2331a`, eyes `#fff6ec→#ffe3c9`.
- Photos must be the EXIF/GPS-stripped copies in `deck/images/fund3/` (already produced by ffmpeg `-map_metadata -1`).
- Mobile first: recipients open from phone mail. Every slide must read at 390px width.
- No commit in thezonebio.com may include its pre-existing untracked `app/dash`, `app/api/dash`, `supabase/.temp`, or the modified `tsconfig.tsbuildinfo`.

---

### Task 1: Deck source skeleton + assets

**Files:**
- Create: `ir/arca-fund3/index.html`
- Create: `ir/arca-fund3/img/` ← copy of `deck/images/fund3/*.jpg`, `demo-2026-06.mp4`, `waitlist-signup.mp4` (drop `_frame_*.jpg`)

**Interfaces:**
- Produces: `index.html` with `<section class="slide" id="s1">…<section id="s11">`, a `<nav class="dots">`, `<div class="progress">`, and JS: `revealOnIntersect()`, `bindKeys()`, `updateProgress()`. Later tasks only edit slide innerHTML.

- [ ] **Step 1: Copy assets**

```bash
cd "/Users/minsungpark/THE ZONE BIO/더존바이오/Project ARCA/arca-wt-brain"
mkdir -p ir/arca-fund3/img
cp deck/images/fund3/*.jpg deck/images/fund3/*.mp4 ir/arca-fund3/img/
rm -f ir/arca-fund3/img/_frame_*.jpg
ls ir/arca-fund3/img | wc -l   # expect 13
```

- [ ] **Step 2: Write skeleton** — `<html lang="ko">`, Pretendard CDN link (same URL as `app/arca/arca.css`), `:root` tokens from Global Constraints, `.deck{scroll-snap-type:y mandatory;overflow-y:auto;height:100dvh}`, `.slide{min-height:100dvh;scroll-snap-align:start;padding:clamp(24px,6vw,72px)}`, `.reveal{opacity:0;transform:translateY(18px);transition:.7s cubic-bezier(.22,.61,.36,1)} .reveal.in{opacity:1;transform:none}`, `@media print{.slide{page-break-after:always;min-height:auto;height:auto} .dots,.progress{display:none}}`. Inline SVG Spirit (`viewBox 0 0 100 100`: fins at (12,62)/(88,62), horn path `M64 16 L78 8 L74 26 Z`, body circle (50,56) r34 with radial gradient, highlight ellipse (38,40), eyes `M28 58 Q28 44 37 44 Q46 44 46 58 Z` / `M54 58 …`) with CSS keyframes `bob 2.6s`, `fin 1.8s`, `blink 3.6s` (scaleY 1→0.08→1 at 90–98%).

- [ ] **Step 3: Verify skeleton renders** — `open ir/arca-fund3/index.html`; screenshot via `screencapture -x /tmp/deck-skel.png` after 2s; confirm 11 empty slides, dots, spirit bobbing (two frames differ).

- [ ] **Step 4: Commit** (arca repo, branch `fund3-deck` off `origin/main`)

```bash
git fetch origin && git checkout -b fund3-deck origin/main
git add ir/arca-fund3 && git commit -m "IR deck (web) skeleton for ZER01NE IR Day 2026-09-21"
```

### Task 2: Slide copy S1–S6 (story half)

**Files:** Modify `ir/arca-fund3/index.html` sections `#s1`–`#s6`.

Copy is the deliverable. Each slide: headline / body / data callout / visual / source footnote.

- [ ] **S1 타이틀** — eyebrow `THE ZONE BIO · ZER01NE Sprint IR Day · 2026.09.21`; h1 `당신의 하루를 기억하고, 대신 끝내는 컴패니언.`; sub `ARCA — 공수 0으로 쌓이는 나의 맥락, 그 맥락으로 대신 처리하는 에이전트`; big Spirit (cursor-follow eyes via `mousemove` → CSS vars `--ex/--ey`); footer `박민성 · me@thezonebio.com · thezonebio.com/arca`.
- [ ] **S2 나는 병목이 됐다** — three message bubbles ("사업계획서 다시 보내주세요" / "견적서 주시면 오늘 결제하겠습니다" / "지난 회의 다음 단계가 뭐였죠?"); punchline `안 하면 일이 멈추고, 하면 내 일이 끊긴다.`; callouts `57% 소통 / 43% 창작`, `68% 방해 없는 집중시간 부족` — source `Microsoft Work Trend Index`. Photo strip: `founders-interview-1.jpg`, `with-jung-nami.jpg` captioned `창업자 40명 인터뷰 (2026 상반기)`.
- [ ] **S3 사용자 0번의 70일: AS-WAS → AS-IS** — two-column before/after (전: 유료 회의록 앱·수동 정리·"그 사람이랑 어디까지 얘기했지?" / 후: 회의→결정·액션아이템 자동, arca it 가능한 건 처리 · 일기를 대신 · 사람·프로젝트별 기억 · 잔일을 먼저 물어봄). Right: app-styled card `회의 요약 · 결정사항 · 액션아이템` rendered from the real 8/13 mentoring summary **excerpt** (PMF 내러티브·타깃·일정 bullets only), caption `ARCA가 8/13 멘토링에서 자동 생성한 회의록 (발췌·실제 출력)`. Footnote `사용 70일 · 축적 1.3GB(8/13 기준) — 창업자 자체 사용 데이터`.
- [ ] **S4 제품: 하나의 흐름** — three cards in a row with connecting arrow: ① 기록 (`회의·통화·대면(ARCA Core)` + `arca-core.jpg` thumb) → ② 기억 (`사람별·프로젝트별 페이지, "○○님과 어디까지?"`) → ③ 위임 (`맥락으로 답장 초안 → "대신 해줄까?" → 승인 → 발송`). Central pill button `arca 해놔`. Under: `데모 영상: 9/17 제출본에 현재 빌드로 촬영해 포함` (honest slot) and small `2026-06 스프린트 데모` thumbnail linking `img/demo-2026-06.mp4`.
- [ ] **S5 왜 지금** — headline `모두가 메모리를 만든다. 묻는 AI는 깔렸다. 끝내는 AI는 비어 있다.`; stat tiles `3만 명 H Chat Pro`, `80% 현대차·기아 임직원`, `-90% 자료 검토 시간`, `연 52.4억 절감` — source footnote `디지털데일리 2026-08-12, EBN 2026 (현대차그룹 AX 보도) — 1차 출처 재확인 예정`. One line: `Dimension(2026-05 폐업): 수동 브리핑은 습관이 안 된다 → ARCA는 하루가 저절로 들어온다.`
- [ ] **S6 해자** — three asymmetry cards (`입력면: 클수록 덜 보여준다` with Recall 회수·개인 위키·ARCA Core / `완수 데이터: 보는 것과 행동하는 것이 한 몸일 때만` / `관계: 함께한 N일 — 애정은 일에서 온다`) + circular loop diagram (SVG, 8 nodes: 하루→맥락→제안→승인→위임범위→시간→애정→더 들여보냄) + measurable line `측정: 사용자별 위임 승인율·자동실행 비율이 날짜와 함께 오르는가`.
- [ ] Verify: `grep -ciE '비타민|second self|방패|flow-state' ir/arca-fund3/index.html` → 0. Commit `IR deck: story slides S1–S6`.

### Task 3: Slide copy S7–S11 (proof half)

- [ ] **S7 트랙션 — 오늘의 사실 / 9·21까지 채울 칸** — left "진짜인 것" list: `사용자 0번 70일 매일 사용 · 1.3GB`, `TestFlight build 12 (9/9) + Mac 베타 배포 시작`, `창업자들이 먼저 다시 연락 — 첫 사용자 신호`, `waitlist 50+ (연세 UIC 총장 포함)`, `40명 인터뷰`, `제로원 인큐베이션팀 베타 사용 중`, `ARCA Core 실물 부팅 (2026-08-05)`. Right "9/21까지" table with empty value cells: 설치 30 · 주간활성 15 · 닫힌 루프 100+ · 유료 10 · 승인율 곡선 — caption `서버 계측(usage_events) 배선 중 — 발표 당일 라이브 대시보드`. Footer honesty line `숫자가 작으면 기울기를 보여드립니다.`
- [ ] **S8 시장·모델** — ladder `창업자·오퍼레이터 → 지식노동자 → 컨슈머`; price cards `Pro ₩12,900 / Team ₩19,900/seat`; unit-econ mini table `STT $2.20 · LLM $0.80 · 인프라 $0.15 · 결제 $0.28 = COGS $3.43 · GM ≈63% (월 10h)`, red note `월 30h↑ 헤비유저 마진 붕괴 → 사용량 캡·팀 요금제`; TAM `AI assistant software $8.46B(2024) → $35.7B(2033), CAGR ≈17% — Grand View Research`. Expansion line marked 가설: `그룹사 1곳 전사 도입 = 3만석 × ₩19,900 × 12 ≈ 연 71.6억 (시나리오)`.
- [ ] **S9 팀** — `team.jpg` hero; three cards (박민성 Founder — 제품·리서치·앱·발표, 사용자 0번 / 박진성 — 하드웨어, 서울대 기계 + 심리학적 사용 설계 / 하승호 — 임베디드, ISEF 임베디드 세계 2등, Caltech); proof strip `knot: Google Cloud × Solana AI Agentic Hackathon 3위 (2026-08-21)`, `6월 이후 74 커밋 / 21 활동일 — macOS·iOS·watchOS·서버·하드웨어`, `ARCA Core first silicon 2026-08-05`; photos `allnight-build.jpg`, `hardware-build.jpg`, `booth.jpg` as small tiles.
- [ ] **S10 Ask** — `5억 · 프리시드 · 12개월`; donut (CSS conic-gradient) 45/30/15/10 with legend (리텐션·제품 / 런웨이·인프라 / 엔터프라이즈 준비(ISMS-P 착수·감사로그·SSO) / 하드웨어 소량 제작); 90일 기준 chips `디자인 파트너 5–10팀 · 반복 사용자 100명 · 유저당 주간 닫힌 루프 · 유료 또는 파일럿 LOI ≥1 · 그룹사 협업 프로젝트 1건 착수`; line `회사 키우는 돈이 아니라, 90일 안에 핵심 가설을 죽이거나 살리는 돈.`
- [ ] **S11 CLOSE** — ladder one-liner `몰입 수호 → 삶의 맥락 → 위임이 쌓는 기억 → 노동에서의 해방 (1층이 안 되면 나머지는 무의미)`; black slide `arca 해놔.`; question `이 팀이 좁은 루프 하나를 끝까지 닫아, 돈 내고 위임하는 습관을 만들 수 있는가 — 90일 안에 데이터로 답하겠습니다.`; contact.
- [ ] Verify grep (banned words) = 0; every footnote present (`grep -c 'class="src"'` ≥ 6). Commit `IR deck: proof slides S7–S11`.

### Task 4: Review lenses (fresh-context, no code changes)

- [ ] Sequoia coverage: table in `ir/arca-fund3/REVIEW.md` mapping 13 elements → slide ids; zero blanks.
- [ ] CEO-review lens (gstack `/plan-ceo-review` prompt: premise challenge, existing leverage, dream state, alternatives, HOLD-SCOPE rigor) run by a fresh subagent on `index.html` copy → findings recorded in `REVIEW.md` with 수용/거부+이유.
- [ ] 심사역 시뮬레이션 ×2 (제로원 3호펀드 CVC 심사역 / 일반 시드 VC) — each returns "투자하지 않는 이유" ranked by severity; fix `high` items in copy; re-run once. Record in `REVIEW.md`.
- [ ] Mobile check: `open -a Safari` responsive mode not scriptable → use `screencapture` of a 390px-wide window via `osascript` resize, or `npx playwright screenshot --viewport-size=390,844`. Fix overflow.
- [ ] Commit `IR deck: review round 1`.

### Task 5: Deploy to thezonebio.com/arcapitch

**Files:**
- Create: `~/.superset/projects/thezonebio.com/public/arcapitch/index.html` + `img/`
- Modify: `~/.superset/projects/thezonebio.com/next.config.ts` (add rewrite after the `/knotpitch` block)

- [ ] Copy: `rsync -a --delete ir/arca-fund3/ ~/.superset/projects/thezonebio.com/public/arcapitch/`
- [ ] Rewrite:

```ts
        // ARCA ZER01NE IR deck — public/arcapitch/index.html 정적 서빙 (knotpitch와 같은 패턴).
        {
          source: "/arcapitch",
          destination: "/arcapitch/index.html",
        },
```

- [ ] Commit only these paths and push:

```bash
cd ~/.superset/projects/thezonebio.com
git add next.config.ts public/arcapitch
git commit -m "ARCA IR deck at /arcapitch (ZER01NE Sprint IR Day 2026-09-21)"
git push origin main
```

- [ ] Verify: poll `curl -sI https://thezonebio.com/arcapitch | head -1` until `200` (Vercel build ~2–3 min); `curl -s https://thezonebio.com/arcapitch | grep -c '<section class="slide"'` = 11; `curl -sI https://thezonebio.com/arcapitch/img/team.jpg | head -1` = 200.

### Task 6: Reply email to mentor + managers

- [ ] Compose Korean reply (합니다체, short): 오늘 멘토링 감사 → IR Deck 초안 링크 `https://thezonebio.com/arcapitch` (모바일 OK, 키보드 ←→) → 구성 요약 3줄 (10분 구조, 트랙션은 오늘 사실+9/21까지 채울 칸, Ask 5억) → 슬라이드별 메모 요청 → 9/17 제출 전 반영 계획. Attach nothing (link only; HTML self-contained available on request).
- [ ] Send via Composio `GMAIL_REPLY_TO_THREAD`: `thread_id=1a07ed77f96c2550`, `recipient_email=jskim@leansprint.kr`, `cc=[jinhwa.lee@hyundai.com, yumi1002@kia.com, cy.an@hyundai.com]`, `is_html=false`, `user_id=<connections.json userId>`.
- [ ] Verify: response `successful: true`; `GMAIL_FETCH_EMAILS query:"thread:1a07ed77f96c2550"` shows the new message from me@thezonebio.com with the 3 cc addresses.

### Task 7: Record + hand-off

- [ ] `deck/arca-zer01ne-fund3-strategy-KR.md`, intent, spec: fix dates (9/19 → 9/21 IR Day, 9/17 제출, 10분).
- [ ] Memory: update `arca-ir-deck-positioning.md` (new hero, dates, URL), `arca-vision-canon-conflict.md` (판결 A–F). membase `add_memory` (project 현대-zero1ne): deck URL, dates, recipients, 판결.
- [ ] Message `arca-d3` session: deck deployed via thezonebio.com only; arca main untouched except `ir/arca-fund3` + docs on branch `fund3-deck`.
